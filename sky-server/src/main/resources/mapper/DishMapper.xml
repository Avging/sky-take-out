<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.avging.mapper.DishMapper">
<!--    useGeneratedKeys="true" 获得插入数据所生成的主键值，keyProperty="id" 把返回的主键值赋给dish类的id，后续返回到业务层实现类中可以通过get获取这个主键值-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dish (name,category_Id,price,image,description,create_time,update_time,create_user,update_user,status)
            VALUES
        (#{name},#{categoryId},#{price},#{image},#{description},#{createTime},#{updateTime},#{createUser},#{updateUser},#{status})
    </insert>

    <!--concat('%',#(name),'%') mysql的字符串连接函数-->
    <select id="pageQuery" resultType="com.avging.vo.DishVO">
        SELECT d.*,c.name AS categoryName FROM dish d LEFT OUTER JOIN category c ON d.category_id = c.id
        <where>
            <if test="name != null">
                AND d.name LIKE concat('%',#(name),'%')
            </if>
            <if test="categoryId != null">
                AND d.category_id = #{category}
            </if>
            <if test="status != null">
                AND d.status = #{status}
            </if>
        </where>
        ORDER BY d.create_time DESC
    </select>

    <delete id="deleteByIds">
        DELETE FROM dish WHERE id IN
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <update id="update">
        UPDATE dish
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="price != null">price = #{price},</if>
            <if test="image != null">image = #{image},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>
